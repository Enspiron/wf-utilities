import SceneReconstructorClient from './scene-reconstructor-client';

export const metadata = {
  title: 'Scene Reconstructor | World Flipper Tools',
  description: 'Explore story scripts, timeline movies, and battle field layers from the datamine.',
};

export default function ScenesPage() {
  return <SceneReconstructorClient />;
}
